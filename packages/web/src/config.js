// Pinned so an upstream icon-set update can't silently change previews/exports.
// Bumping the version changes the manifest cache key, invalidating a stale cache.
export const ICON_VERSION = '1.0.33';
export const ICON_STYLE = 'round';
